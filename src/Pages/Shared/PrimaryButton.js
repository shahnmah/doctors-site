import React from 'react';

const PrimaryButton = () => {
    return (
        <div>
            <button class="btn bg-gradient-to-r from-secondary to-primary border-0 text-white font-bold">Get Started</button>
        </div>
    );
};

export default PrimaryButton;